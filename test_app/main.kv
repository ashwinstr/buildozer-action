MainWidget:

#:set btn_font_h 30

<MainWidget>:
    orientation: 'vertical'
    Label:
        color: 0, 0, 0, 1
        text: root.total
        font_name: "resources/digital-7.ttf"
        font_size: sp(80)
        size_hint: 1, 1/3
    Label:
        id: input
        text: root.equation
        color: 0, 0, 0, 0.75
        font_size: sp(30)
        size_hint: 1, 1/3
    ComputeBox:
        size_hint: 1, 1/2
        BoxLayout:
            orientation: 'vertical'
            size_hint: 3/5, 1
            NumButtons:
                id: num_buttons
                text: self.text
        BoxLayout:
            orientation: 'vertical'
            size_hint: 2/5, 1
            padding: dp(10)
            spacing: dp(10)
            MathButtons:
                id: math_buttons
